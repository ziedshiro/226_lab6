#include<stdio.h>
int main()
{
    int n,q;
    scanf("%d %d",&n,&q);
    int s[n],i,a[q],b[q],c[q];
    for(i=0;i<n;i++)
        s[i]=0;
    for(i=0;i<q;i++)
    {
        scanf("%s",&a[i]);
        scanf("%d",&b[i]);
        scanf("%d",&c[i]);
    }

}
